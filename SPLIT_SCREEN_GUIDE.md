# 🖥️ 2분할 화면 UI 가이드

## 📋 개요
의료 검사 리포트 생성기의 **2분할 화면 UI**가 구현되었습니다.

## 🎨 화면 구성

### 좌측 패널 (40% 너비)
**파란색 그라데이션 배경**

1. **📎 파일 업로드**
   - 클릭하거나 드래그 앤 드롭으로 엑셀 파일 업로드
   - 지원 형식: `.xlsx`, `.xls`

2. **👤 환자 정보 (선택)**
   - 환자명
   - 환자번호

3. **👨‍⚕️ 의료진 코멘트 (선택)**
   - 검사 결과에 대한 코멘트 입력

4. **📥 출력 형식**
   - PDF (권장)
   - PNG (이미지)
   - HTML

5. **📥 리포트 생성 및 다운로드 버튼**

### 우측 패널 (60% 너비)
**흰색 배경 - 실시간 미리보기**

엑셀 파일을 업로드하면 **자동으로** 다음 정보가 표시됩니다:

#### 📌 검사 결과 요약
- **총 검사 항목**: 파란색 박스
- **정상**: 초록색 박스
- **주의**: 빨간색 박스

#### ⚠️ 주의가 필요한 항목 (이상 항목이 있을 경우만)
- 카테고리별 이상 항목 개수 표시

#### 💡 권장사항
- AI가 분석한 권장사항 목록

## 🔄 동작 흐름

```
1. 사용자가 좌측에서 엑셀 파일 선택
   ↓
2. JavaScript가 /preview API 호출
   ↓
3. 백엔드에서 파일 분석 (파싱 → 분석)
   ↓
4. 우측 패널에 실시간으로 결과 표시
   ↓
5. 사용자가 추가 정보 입력 (선택)
   ↓
6. "리포트 생성" 버튼 클릭
   ↓
7. PDF/PNG 다운로드
```

## 🌐 API 엔드포인트

### `/preview` (POST)
**실시간 미리보기용**
- 입력: `file` (multipart/form-data)
- 출력: JSON
  ```json
  {
    "success": true,
    "patient_info": {...},
    "summary": {
      "total": 55,
      "normal": 45,
      "abnormal": 10,
      "abnormal_groups": [...]
    },
    "recommendations": [...]
  }
  ```

### `/upload` (POST)
**최종 리포트 생성용**
- 입력: `file`, `patient_name`, `patient_id`, `doctor_comment`, `output_format`
- 출력: 파일 다운로드 (PDF/PNG/HTML)

## 📱 반응형 디자인

### 데스크톱 (>1024px)
```
┌─────────────────────────────────────┐
│  좌측 (40%)     │    우측 (60%)    │
│                 │                   │
│  [업로드 폼]    │  [실시간 미리보기]│
│                 │                   │
└─────────────────────────────────────┘
```

### 태블릿/모바일 (≤1024px)
```
┌─────────────────┐
│   좌측 패널     │ 50vh
├─────────────────┤
│   우측 패널     │ 나머지
└─────────────────┘
```

## 🎯 주요 기능

✅ **드래그 앤 드롭** 파일 업로드  
✅ **실시간 미리보기** (파일 선택 즉시 분석)  
✅ **로딩 상태** 표시 (스피너 애니메이션)  
✅ **에러 핸들링** (파일 형식 검증, 분석 실패 처리)  
✅ **자동 다운로드** (리포트 생성 완료 시)  
✅ **반응형 레이아웃** (모바일/태블릿/데스크톱)

## 🚀 사용 방법

1. **웹 서비스 접속**
   ```
   https://5000-i2ec6aqmnzggmozif9qdy-18e660f9.sandbox.novita.ai
   ```

2. **좌측 패널에서 엑셀 파일 업로드**
   - 클릭 또는 드래그 앤 드롭

3. **우측 패널에서 실시간 미리보기 확인**
   - 자동으로 검사 결과 요약 표시
   - 권장사항 확인

4. **필요 시 추가 정보 입력** (선택)
   - 환자명, 환자번호
   - 의료진 코멘트

5. **출력 형식 선택 (기본: PDF)**

6. **"리포트 생성 및 다운로드" 버튼 클릭**

## 🎨 디자인 특징

### 색상 팔레트
- **좌측 패널 배경**: 보라색 그라데이션 (`#667eea` → `#764ba2`)
- **총 검사 항목**: 차분한 파란색 (`#5b8db8`)
- **정상**: 차분한 초록색 (`#5a9367`)
- **주의**: 차분한 코랄 (`#c9665f`)

### 그림자 효과
- **카드**: `0 2px 8px rgba(0,0,0,0.1)`
- **통계 박스**: 그라데이션 + 그림자
- **우측 패널**: `0 -5px 15px rgba(0,0,0,0.1)`

## 📊 테스트 결과

### 테스트 파일
- **파일명**: `도희석_검체검사결과_20260122.xlsx`
- **총 검사 항목**: 55개
- **정상**: 45개
- **주의**: 10개

### 미리보기 표시 항목
✅ 검사 결과 요약 (통계)  
✅ 주의가 필요한 항목 (카테고리별)  
✅ 권장사항 목록  

### 최종 출력
✅ PDF 생성: 약 75-80 KB  
✅ PNG 생성: 약 570-580 KB  
✅ HTML 생성: 약 50-60 KB

## 🔧 기술 스택

### 프론트엔드
- **HTML5** + **CSS3** (Flexbox, Grid)
- **Vanilla JavaScript** (Fetch API)
- **반응형 디자인** (Media Queries)

### 백엔드
- **Flask** (Python 웹 프레임워크)
- **Openpyxl** (엑셀 파싱)
- **WeasyPrint** (PDF 생성)
- **PyMuPDF** (PNG 생성)

## 📝 향후 개선 가능 사항

1. **이미지 미리보기**
   - PDF를 이미지로 변환하여 우측에 표시

2. **실시간 편집**
   - 미리보기에서 직접 권장사항 수정

3. **다국어 지원**
   - 한국어/영어 전환

4. **테마 선택**
   - 라이트/다크 모드

5. **히스토리 관리**
   - 최근 생성한 리포트 목록

## 🎉 완료!

**2분할 화면 UI**가 완전히 구현되었습니다!  
좌측에서 파일을 업로드하면 우측에서 **실시간으로** 검사 결과를 확인할 수 있습니다.

---

**웹 서비스 URL**:  
https://5000-i2ec6aqmnzggmozif9qdy-18e660f9.sandbox.novita.ai

**GitHub 저장소**:  
https://github.com/gwmaw/-

**Pull Request**:  
https://github.com/gwmaw/-/pull/1
